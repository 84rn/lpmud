*Zdefiniowane w /sys/global/debug.h

DEBUG_EVALCOST			Zwraca dotychczasowy naliczony koszt
				wykonywania (eval_cost) aktualnego ciagu
				wywolan.

#define DEBUG_EVALCOST	(SECURITY->do_debug("get_eval_cost"))
