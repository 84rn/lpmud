*Zdefiniowane w /sys/composite.h

/*
 * COMPOSITE_WORDS
 *
 * Zwykle polaczenie slow z listy przecinkami, a ostatniego slowem 'i'.
 */
#define COMPOSITE_WORDS(wl)     call_other(COMPOSITE_FILE, "composite_words", wl)

