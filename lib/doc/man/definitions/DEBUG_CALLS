*Zdefiniowane w /sys/global/debug.h

DEBUG_CALLS(int start)  Zwraca tablice:
                        ({({DEBUG_CFUNS[0], DEBUG_COBJS[0], DEBUG_CPROGS[0]}),
                          ({CFUNS[1], COBJS[1], CPROGS[1]}), ... }).
                        start - jak w DEBUG_CFUNS/COBJS/CPROGS.
                        Uwaga: Uzywanie funkcji calling_* jest dosc kosztowne,
                               wiec prosimy o nie naduzywanie tego makra.
