*Defined in /sys/language.h

LANG_ASHORT(ob)		Returns the short description for ob with a or an
			if the object is not a heap. If heap returns the 
			normal short form, which has a or an in front of it
			if a heap of just one item.

